import { DisplayFechaPipe } from './display-fecha.pipe';

describe('DisplayFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
