import { OrderByFechaPipe } from './order-by-fecha.pipe';

describe('OrderByFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
