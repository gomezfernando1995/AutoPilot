import { DisplayHoraPipe } from './display-hora.pipe';

describe('DisplayHoraPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayHoraPipe();
    expect(pipe).toBeTruthy();
  });
});
