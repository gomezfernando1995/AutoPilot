import { FiltrarDuplicadoPipePipe } from './filtrar-duplicado-pipe.pipe';

describe('FiltrarDuplicadoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarDuplicadoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
